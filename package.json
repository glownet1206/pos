{
  "name": "pos-system",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm rebuild sqlite3",
    "start": "node backend/server.js"
  }
}
